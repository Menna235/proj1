import React from "react";
//import logo from "../logo.svg";
import logo from "../logo.svg";

const Image = ({ menna }) => {
  return <img src={menna} className="App-logo" alt="logo" />;
};

export default Image;
